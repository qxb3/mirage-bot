const { MessageEmbed } = require('discord.js')

module.exports = {
    category: 'Test',
    description: 'Testing pagination',
    testOnly: true,

    callback: async ({ message, channel }) => {
    }
}
